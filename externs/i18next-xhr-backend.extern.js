/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/i18next-xhr-backend/index.d.ts:
/** @const */
var I18NextXhrBackend = {};

/** @typedef {(string|function(!Array<string>, !Array<string>): string)} */
I18NextXhrBackend.LoadPathOption;
/**
 * @record
 * @struct
 */
I18NextXhrBackend.BackendOptions = function() {};
 /** @type {(string|function(!Array<string>, !Array<string>): string)} */
I18NextXhrBackend.BackendOptions.prototype.loadPath;
 /** @type {string} */
I18NextXhrBackend.BackendOptions.prototype.addPath;
 /** @type {boolean} */
I18NextXhrBackend.BackendOptions.prototype.allowMultiLoading;
 /** @type {boolean} */
I18NextXhrBackend.BackendOptions.prototype.crossDomain;
 /** @type {boolean} */
I18NextXhrBackend.BackendOptions.prototype.withCredentials;
 /** @type {!Object<string,string>} */
I18NextXhrBackend.BackendOptions.prototype.queryStringParams;
 /** @type {!Object<string,string>} */
I18NextXhrBackend.BackendOptions.prototype.customHeaders;

/**
 * parse data after it has been fetched
 * in example use https://www.npmjs.com/package/json5
 * here it removes the letter a from the json (bad idea)
 * @param {string} data
 * @return {string}
 */
I18NextXhrBackend.BackendOptions.prototype.parse = function(data) {};

/**
 * define a custom xhr function
 * can be used to support XDomainRequest in IE 8 and 9
 * @param {string} url
 * @param {!I18NextXhrBackend.BackendOptions} options
 * @param {function(string, !XMLHttpRequest): void} callback
 * @param {(string|!Object)} data
 * @param {boolean} cache
 * @return {void}
 */
I18NextXhrBackend.BackendOptions.prototype.ajax = function(url, options, callback, data, cache) {};

/** @typedef {function(string, !XMLHttpRequest): void} */
I18NextXhrBackend.AjaxRequestCallback;

/** @typedef {function(?, (string|boolean)): void} */
I18NextXhrBackend.LoadCallback;
 /** @type {string} */
I18NextXhrBackend.prototype.type;
 /** @type {?} */
I18NextXhrBackend.prototype.services;
 /** @type {!I18NextXhrBackend.BackendOptions} */
I18NextXhrBackend.prototype.options;

/**
 * @param {!I18NextXhrBackend.BackendOptions=} options
 * @return {void}
 */
I18NextXhrBackend.prototype.init = function(options) {};

/**
 * @param {!Array<string>} languages
 * @param {!Array<string>} namespaces
 * @param {function(?, (string|boolean)): void} callback
 * @return {void}
 */
I18NextXhrBackend.prototype.readMulti = function(languages, namespaces, callback) {};

/**
 * @param {string} language
 * @param {string} namespace
 * @param {function(?, (string|boolean)): void} callback
 * @return {void}
 */
I18NextXhrBackend.prototype.read = function(language, namespace, callback) {};

/**
 * @param {string} url
 * @param {function(?, (string|boolean)): void} callback
 * @return {void}
 */
I18NextXhrBackend.prototype.loadUrl = function(url, callback) {};

/**
 * @param {(string|!Array<string>)} languages
 * @param {string} namespace
 * @param {string} key
 * @param {string} fallbackValue
 * @return {void}
 */
I18NextXhrBackend.prototype.create = function(languages, namespace, key, fallbackValue) {};

/* TODO: ExportAssignment in  */
