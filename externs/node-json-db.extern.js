/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/node-json-db/index.d.ts:

/**
 * @constructor
 * @struct
 * Create the JSON database
 * @param {string} filename where to save the data base
 * @param {boolean=} saveOnPush saving on modification of the data
 * @param {boolean=} humanReadable is the json file humand readable 
 */
function JsonDB(filename, saveOnPush, humanReadable) {}

/**
 * Get the deta stored in the data base
 * @param {string} dataPath path leading to the data
 * @return {?}
 */
JsonDB.prototype.getData = function(dataPath) {};

/**
 * Pushing data into the database
 * @param {string} dataPath path leading to the data
 * @param {?} data data to push
 * @param {boolean=} override overriding or not the data, if not, it will merge them
 * @return {void}
 */
JsonDB.prototype.push = function(dataPath, data, override) {};

/**
 * Delete the data
 * @param {string} dataPath path leading to the data
 * @return {void}
 */
JsonDB.prototype.delete = function(dataPath) {};

/**
 * Reload the database from the file
 * @return {void}
 */
JsonDB.prototype.reload = function() {};

/**
 * Manually load the database
 * It is automatically called when the first getData is done
 * @return {void}
 */
JsonDB.prototype.load = function() {};

/**
 * Manually save the database
 * By default you can't save the database if it's not loaded
 * @param {boolean=} force force the save of the database
 * @return {void}
 */
JsonDB.prototype.save = function(force) {};

/* TODO: ExportAssignment in  */
