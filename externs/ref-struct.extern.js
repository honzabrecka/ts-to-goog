/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/ref-struct/index.d.ts:

/* TODO: ImportEqualsDeclaration in  */
/**
 * @extends {Type}
 * @record
 * @struct
 */
function StructType() {}

/* TODO: ConstructSignature:  */

/* TODO: ConstructSignature:  */

/* TODO: CallSignature:  */

/* TODO: CallSignature:  */
 /** @type {?} */
StructType.prototype.fields;

/**
 * Adds a new field to the struct instance with the given name and type.
 * Note that this function will throw an Error if any instances of the struct
 * type have already been created, therefore this function must be called at the
 * beginning, before any instances are created.
 * Adds a new field to the struct instance with the given name and type.
 * Note that this function will throw an Error if any instances of the struct
 * type have already been created, therefore this function must be called at the
 * beginning, before any instances are created.
 * @param {string} name
 * @param {?|string} type
 * @return {void}
 */
StructType.prototype.defineProperty = function(name, type) {};

/**
 * Custom for struct type instances.
 * @override
 * @return {string}
 */
StructType.prototype.toString = function() {};
 /** @type {function(new: (?), !Object): ?} */
var StructType;

/* TODO: ExportAssignment in  */
