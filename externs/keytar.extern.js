/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/keytar/index.d.ts:

/**
 * Get the stored password for the service and account.
 * 
 * @param {string} service The string service name.
 * @param {string} account The string account name.
 * 
 * @return {!Promise<string>} A promise for the password string.
 */
function getPassword(service, account) {}

/**
 * Add the password for the service and account to the keychain.
 * 
 * @param {string} service The string service name.
 * @param {string} account The string account name.
 * @param {string} password The string password.
 * 
 * @return {!Promise<void>} A promise for the set password completion.
 */
function setPassword(service, account, password) {}

/**
 * Delete the stored password for the service and account.
 * 
 * @param {string} service The string service name.
 * @param {string} account The string account name.
 * 
 * @return {!Promise<boolean>} A promise for the deletion status. True on success.
 */
function deletePassword(service, account) {}

/**
 * Find a password for the service in the keychain.
 * 
 * @param {string} service The string service name.
 * 
 * @return {!Promise<string>} A promise for the password string.
 */
function findPassword(service) {}
