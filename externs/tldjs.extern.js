/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/tldjs/index.d.ts:
 /** @type {!Array<string>} */
var validHosts;

/**
 * Checks if the TLD is valid for a given host.
 * @param {string} host The TLD/host/url to check
 * @return {boolean}
 */
function tldExists(host) {}

/**
 * Returns the fully qualified domain from a host string.
 * @param {string} host The host/url to check
 * @return {string}
 */
function getDomain(host) {}

/**
 * Returns the complete subdomain for a given host.
 * @param {string} host The host/url to check
 * @return {string}
 */
function getSubdomain(host) {}

/**
 * Returns the public suffix (including exact matches)
 * @param {string} host The host/url to check
 * @return {string}
 */
function getPublicSuffix(host) {}

/**
 * Checking if a host string is valid
 * It's usually a preliminary check before trying to use getDomain or anything else
 * Beware: it does not check if the TLD exists.
 * @param {string} host The host/url to check
 * @return {string}
 */
function isValid(host) {}
