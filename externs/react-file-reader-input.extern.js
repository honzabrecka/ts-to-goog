/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/react-file-reader-input/index.d.ts:

/* TODO: ImportDeclaration in  */
/**
 * @extends {React.Component}
 * @constructor
 * @struct
 */
function FileInput() {}

/** @typedef {string} */
FileInput.Format;

/** @typedef {!Array<?>} */
FileInput.Result;
/**
 * @record
 * @struct
 */
FileInput.Props = function() {};
 /** @type {string} */
FileInput.Props.prototype.as;

/**
 * Callback function called when the files are choosen by the user.
 * 
 * Results will be an array of arrays, the size of which depending
 * on how many files were selected.
 * 
 * Each result will be an array of two items:
 * 
 * `progressEvent`: `result[0]` is a `ProgressEvent` object.
 * You can retrieve the raw results at `progressEvent.target.result`
 * among other things.
 * 
 * `file`: `result[1]` is a `File` object. You can retrieve the file name
 * at file.name among other things.
 * 
 * @param {!React.SyntheticEvent<?>} event The event that triggered file changes
 * @param {!Array<!Array<?>>} results The array of files
 * @return {void}
 */
FileInput.Props.prototype.onChange = function(event, results) {};

/* TODO: ExportAssignment in  */
