/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/mithril-global/index.d.ts:

/* TODO: ImportDeclaration in  */

/* TODO: ImportDeclaration in  */
/** @const */
var MithrilGlobal = {};

/** @typedef {!Mithril.Lifecycle<A, S>} */
MithrilGlobal.Lifecycle;

/** @typedef {!Mithril.Hyperscript} */
MithrilGlobal.Hyperscript;

/** @typedef {!Mithril.RouteResolver<S, P>} */
MithrilGlobal.RouteResolver;

/** @typedef {!Mithril.RouteDefs} */
MithrilGlobal.RouteDefs;

/** @typedef {!Mithril.RouteOptions} */
MithrilGlobal.RouteOptions;

/** @typedef {!Mithril.Route} */
MithrilGlobal.Route;

/** @typedef {!Mithril.RequestOptions<T>} */
MithrilGlobal.RequestOptions;

/** @typedef {!Mithril.JsonpOptions} */
MithrilGlobal.JsonpOptions;

/** @typedef {(string|number|boolean|!Mithril.Vnode<?, ?>)} */
MithrilGlobal.Child;

/** @typedef {!Mithril.ChildArray} */
MithrilGlobal.ChildArray;

/** @typedef {(string|number|boolean|!Mithril.Vnode<?, ?>|!Mithril.ChildArray)} */
MithrilGlobal.Children;

/** @typedef {(string|number|boolean|!Mithril.ChildArray)} */
MithrilGlobal.ChildArrayOrPrimitive;

/** @typedef {!Mithril.Vnode<A, S>} */
MithrilGlobal.Vnode;

/** @typedef {!Mithril.VnodeDOM<A, S>} */
MithrilGlobal.VnodeDOM;

/** @typedef {!Mithril.CVnode<A>} */
MithrilGlobal.CVnode;

/** @typedef {!Mithril.CVnodeDOM<A>} */
MithrilGlobal.CVnodeDOM;

/** @typedef {!Mithril.Component<A, S>} */
MithrilGlobal.Component;

/** @typedef {?} */
MithrilGlobal.Comp;

/** @typedef {!Mithril.ClassComponent<A>} */
MithrilGlobal.ClassComponent;

/** @typedef {function(!Mithril.Vnode<A, !Object>): !Mithril.Component<A, ?>} */
MithrilGlobal.FactoryComponent;

/** @typedef {(!Mithril.Component<A, S>|function(new: (!Mithril.ClassComponent<A>), !Mithril.CVnode<A>): ?|function(!Mithril.Vnode<A, !Object>): !Mithril.Component<A, ?>)} */
MithrilGlobal.ComponentTypes;

/** @typedef {!Mithril.Attributes} */
MithrilGlobal.Attributes;

/** @typedef {!Stream.Stream<T>} */
MithrilGlobal.Stream;

/** @typedef {?} */
MithrilGlobal.Static;
 /** @type {?} */
var MithrilGlobal;

/* TODO: ExportAssignment in  */

/* TODO: NamespaceExportDeclaration in  */
