/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/topojson/index.d.ts:

/**
 * @param {?} topology
 * @return {?}
 */
function bbox(topology) {}

/**
 * @param {?} topology
 * @param {?} o
 * @return {?}
 */
function feature(topology, o) {}

/**
 * @param {?} topology
 * @param {?} filter
 * @return {?}
 */
function filter(topology, filter) {}

/**
 * @param {?} topology
 * @return {?}
 */
function filterAttached(topology) {}

/**
 * @param {?} topology
 * @param {?} minWeight
 * @param {?} weight
 * @return {?}
 */
function filterAttachedWeight(topology, minWeight, weight) {}

/**
 * @param {?} topology
 * @param {?} minWeight
 * @param {?} weight
 * @return {?}
 */
function filterWeight(topology, minWeight, weight) {}

/**
 * @param {?} topology
 * @param {...?} args
 * @return {?}
 */
function merge(topology, args) {}

/**
 * @param {?} topology
 * @param {?} objects
 * @return {?}
 */
function mergeArcs(topology, objects) {}

/**
 * @param {?} topology
 * @param {...?} args
 * @return {?}
 */
function mesh(topology, args) {}

/**
 * @param {?} topology
 * @param {?} object$$1
 * @param {?} filter
 * @param {...?} args
 * @return {?}
 */
function meshArcs(topology, object$$1, filter, args) {}

/**
 * @param {?} objects
 * @return {!Array<?>}
 */
function neighbors(objects) {}

/**
 * @param {?} ring
 * @return {?}
 */
function planarRingArea(ring) {}

/**
 * @param {?} triangle
 * @return {?}
 */
function planarTriangleArea(triangle) {}

/**
 * @param {?} topology
 * @param {?} weight
 * @return {!Array<?>}
 */
function presimplify(topology, weight) {}

/**
 * @param {?} topology
 * @param {?} p
 * @return {?}
 */
function quantile(topology, p) {}

/**
 * @param {?} topology
 * @param {?} transform
 * @return {?}
 */
function quantize(topology, transform) {}

/**
 * @param {?} topology
 * @param {?} minWeight
 * @return {?}
 */
function simplify(topology, minWeight) {}

/**
 * @param {?} ring
 * @param {?} interior
 * @return {?}
 */
function sphericalRingArea(ring, interior) {}

/**
 * @param {?} t
 * @return {?}
 */
function sphericalTriangleArea(t) {}

/**
 * @param {?} objects
 * @param {?} quantization
 * @return {?}
 */
function topology(objects, quantization) {}

/**
 * @param {?} transform
 * @return {?}
 */
function transform(transform) {}

/**
 * @param {?} transform
 * @return {?}
 */
function untransform(transform) {}
