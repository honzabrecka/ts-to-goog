/**
 * @externs
 * @suppress {duplicate,checkTypes}
 */
// NOTE: generated by tsickle, do not edit.
// externs from d/pinkyswear/index.d.ts:
/** @const */
var PinkySwear = {};
/**
 * @record
 * @struct
 */
PinkySwear.Promise = function() {};

/* TODO: CallSignature: PinkySwear */

/* TODO: CallSignature: PinkySwear */

/**
 * onFulfilled is called when or if the promise is resolved.
 * onRejected is called when or if the promise is rejected.
 * 
 * @param {function(!Array<?>): ?=} onFulfilled Called when or if the promise is resolved.
 * @param {function(!Array<?>): void=} onRejected Called when or if the promise is rejected.
 * @return {!PinkySwear.Promise} PinkySwear.Promise
 */
PinkySwear.Promise.prototype.then = function(onFulfilled, onRejected) {};
/**
 * @extends {PinkySwear.Promise}
 * @record
 * @struct
 */
PinkySwear.GenericPromise = function() {};

/* TODO: CallSignature: PinkySwear */

/**
 * onFulfilled is called when or if the promise is resolved.
 * onRejected is called when or if the promise is rejected.
 * 
 * onFulfilled is called when or if the promise is resolved.
 * onRejected is called when or if the promise is rejected.
 * 
 * onFulfilled is called when or if the promise is resolved.
 * onRejected is called when or if the promise is rejected.
 * 
 * onFulfilled is called when or if the promise is resolved.
 * onRejected is called when or if the promise is rejected.
 * 
 * onFulfilled is called when or if the promise is resolved.
 * onRejected is called when or if the promise is rejected.
 * 
 * onFulfilled is called when or if the promise is resolved.
 * onRejected is called when or if the promise is rejected.
 * 
 * @template G
 * @param {function(T): G|function(T): !PinkySwear.GenericPromise<G>|function(T): void=} onFulfilled Called when or if the promise is resolved.
 * @param {function(T): void|function(!TypeError): void=} onRejected Called when or if the promise is rejected.
 * @return {!PinkySwear.GenericPromise<G>|!PinkySwear.Promise} PinkySwear.GenericPromise / PinkySwear.Promise
 */
PinkySwear.GenericPromise.prototype.then = function(onFulfilled, onRejected) {};
/**
 * @record
 * @struct
 */
function PinkySwearStatic() {}

/* TODO: CallSignature:  */

/* TODO: CallSignature:  */

/* TODO: CallSignature:  */

/* TODO: CallSignature:  */
 /** @type {!PinkySwearStatic} */
var pinkySwear;
